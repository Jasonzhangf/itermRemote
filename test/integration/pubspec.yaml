name: integration_tests
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  cloudplayplus_core:
    path: ../../packages/cloudplayplus_core
  iterm2_host:
    path: ../../packages/iterm2_host
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

