name: cloudplayplus_core
version: 0.1.0
description: Shared core library for iTerm2 remote streaming
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.26.0
  mockito: ^5.4.4
  build_runner: ^2.4.0
