name: iterm2_host
version: 0.1.0
description: macOS host service for iTerm2 remote streaming
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  cloudplayplus_core:
    path: ../cloudplayplus_core
  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.26.0
  mockito: ^5.4.4
