name: itermremote_blocks
description: Block interfaces and registry for iTermRemote daemon.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  itermremote_protocol:
    path: ../itermremote_protocol
  iterm2_host:
    path: ../iterm2_host

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
